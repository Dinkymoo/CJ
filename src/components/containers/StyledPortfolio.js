import styled from 'styled-components';

const StyledPortfolio = styled.div`
  background: ${props => props.theme.bgcolour};
  color: ${props => props.theme.fontcolour};
`;

export default StyledPortfolio;
